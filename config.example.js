// Скопируйте этот файл в config.js и заполните значения.
// config.js НЕ попадает в репозиторий (.gitignore).
// Без config.js приложение работает с дефолтными значениями из integrations/config.js.

window.APP_CONFIG = {
  // Supabase — URL проекта и anon-ключ
  supabaseUrl:     '',
  supabaseAnonKey: '',

  // Google OAuth Client ID (для экспорта в Google Sheets)
  googleClientId:  '',
};
